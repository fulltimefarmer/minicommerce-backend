spring:
  config:
    activate:
      on-profile: Local
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/demo?characterEncoding=utf-8&useSSL=false
    username: admin
    password: 123456
        